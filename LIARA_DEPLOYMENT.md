# راهنمای جامع استقرار در لیارا (Liara Deployment Guide)
### مخصوص وایب‌کدینگ (Vibe Coding) و توسعه‌دهندگان سریع

این پروژه به صورت کامل و اختصاصی برای اجرا و استقرار بدون دغدغه در پلتفرم ابری **لیارا (Liara PaaS)** طراحی شده است. معماری پروژه فول‌استک (Node.js + Express + Vite React) با پورت استاندارد `3000` و دیتابیس پایدار فایل‌محور در پوشه `data/` است.

---

## ۱. پیش‌نیازهای اولیه در سیستم شما
1. داشتن حساب کاربری در [سایت لیارا](https://liara.ir).
2. نصب Node.js (نسخه ۱۸ یا بالاتر).
3. نصب ابزار خط فرمان لیارا (Liara CLI):
```bash
npm install -g @liara/cli
```

---

## ۲. ورود به حساب لیارا از طریق ترمینال
در ترمینال سیستم خود دستور زیر را وارد کنید و مشخصات حساب کاربری لیارای خود را وارد نمایید:
```bash
liara login
```

---

## ۳. ایجاد برنامه در کنسول لیارا
1. وارد پنل کاربری لیارا شوید و روی دکمه **«ایجاد برنامه»** کلیک کنید.
2. پلتفرم را **NodeJS** انتخاب کنید.
3. یک شناسه برای برنامه خود بنویسید (مثلاً: `aria-casino`).
4. پلن سخت‌افزاری مورد نظر خود را انتخاب کرده و برنامه را ایجاد کنید.

---

## ۴. ایجاد دیسک پایدار (Persistent Disk) برای دیتابیس
> ⚠️ **خیلی مهم برای حفظ کاربران، موجودی‌ها و لاگ‌های ادمین:**
> از آنجا که لیارا محیط کانتینری Stateless دارد، برای اینکه اطلاعات ثبت‌نامی کاربران و لاگ‌های ادمین با ری‌استارت برنامه پاک نشود، یک دیسک در لیارا ایجاد کنید:

1. در پنل برنامه در لیارا، به بخش **دیسک‌ها (Disks)** بروید.
2. روی **«ایجاد دیسک»** کلیک کنید.
3. نام دیسک را `data` بگذارید و حجم را مثلاً ۱ یا ۵ گیگابایت انتخاب کنید.
4. مسیر اتصال (Mount Path) را روی `data` یا `/app/data` بگذارید.
   *(فایل `liara.json` از قبل در این پروژه آماده شده و اتصال دیسک `data` را به صورت خودکار تنظیم می‌کند).*

---

## ۵. استقرار تنها با یک دستور (Deploy)
ترمینال را در پوشه اصلی پروژه باز کرده و دستور زیر را تایپ کنید:
```bash
liara deploy
```
ابزار لیارا به صورت خودکار فایل `liara.json` را خوانده، پلتفرم Node.js و پورت ۳۰۰۰ را شناسایی کرده و فرآیند بیلد و اجرا را انجام می‌دهد:
- فرآیند بیلد خودکار: `npm run build`
- فرآیند اجرا: `npm start` (که فایل سرور سریع `dist/server.cjs` را اجرا می‌کند).

---

## ۶. تنظیم متغیرهای محیطی (اختیاری)
اگر می‌خواهید رمز پیش‌فرض ادمین یا تنظیمات دیگر را تغییر دهید، می‌توانید در بخش «متغیرهای محیطی» لیارا تنظیم کنید:
- `NODE_ENV=production`
- `PORT=3000`

---

## ۷. مشخصات پیش‌فرض پنل ادمین
- **نام کاربری ادمین:** `admin`
- **رمز عبور ادمین:** `admin123`
- **کد احراز هویت دو مرحله‌ای (2FA):** `778899`
- کاربر عادی پیش‌فرض: `player1` با رمز `user123` (موجودی اولیه: ۰ تومان).
*(هر کاربر جدیدی هم ثبت نام کند، دقیقاً با موجودی ۰ تومان ساخته می‌شود و ادمین می‌تواند از پنل مدیریت حساب او را شارژ کند یا از دکمه شارژ تست استفاده نماید).*

---

## ۸. قابلیت‌های اختصاصی پنل ادمین
- **مشاهده و مدیریت کاربران**: ویرایش موجودی (+ یا - یا مقدار دلخواه)، مسدود/فعال‌سازی حساب.
- **کنترل نرخ برد (RTP)**: تنظیم شانس برد بازی‌های انفجار، رولت، اسلات، مین‌ها و طاس.
- **کنترل و مهندسی دستی نتایج (Rigging / Manual Force)**:
  - تعیین ضریب بعدی بازی انفجار (مثلاً ۱.۰۰ برای باخت آنی یا ۱۰.۰۰ برای سود بالا).
  - تعیین عدد برنده بعدی رولت (بین ۰ تا ۳۶).
  - تعیین نتیجه بعدی اسلات (جک‌پات، مگاوین یا باخت).
  - تعیین خانه انفجار بازی مین‌ها.
- **لاگ‌های جامع امنیتی ادمین**: ثبت لحظه‌ای تمام تراکنش‌ها، تغییرات ضرایب و ورودها در دیتابیس.
